version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      # Keep vitest + @vitest/* on the same major; separate bumps often break npm ci / CI.
      vitest:
        patterns:
          - 'vitest'
          - '@vitest/*'
